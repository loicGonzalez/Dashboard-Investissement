Idéés pour rendre l'application accessible depuis mon smartphone:

Accès Wi‑Fi local (0.0.0.0 + IP du Mac)
Puis Tailscale (ou équivalent) pour le mobile hors maison
Auth simple si un jour tu exposes l’app plus largement

En attendant, l’app reste optimisée pour usage local sur le Mac.